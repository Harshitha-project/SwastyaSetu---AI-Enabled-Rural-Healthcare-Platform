// SwasthyaSetu - Database Models Export
// Using Prisma ORM with PostgreSQL

// Export Prisma client for database access
export { prisma } from '../config/database'

// Note: All database models are now defined in prisma/schema.prisma
// Import and use them directly from @prisma/client:
// 
// import { PrismaClient, User, Patient, Doctor, etc. } from '@prisma/client'
// 
// Or use the prisma instance exported above:
// import { prisma } from './models'
// const users = await prisma.user.findMany()
